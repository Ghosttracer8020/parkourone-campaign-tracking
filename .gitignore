# Reference material — sibling plugins & theme have their own git repos
Input/

# macOS
.DS_Store

# Node / build (if added later)
node_modules/
vendor/

# SHA file written by the GitHub self-updater
.git-version
