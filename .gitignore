# Reference material — sibling plugins & theme have their own git repos
Input/

# macOS
.DS_Store

# Node / build (if added later)
node_modules/
vendor/

# SHA file written by the GitHub self-updater
.git-version

# GSD planning + internal workspace docs — kept local only, NOT part of the
# distributed plugin (the updater deploys the whole repo into the plugin folder).
.planning/
CONTEXT-FINDINGS.md
MANUAL-VERIFICATION.md
CLAUDE.md
